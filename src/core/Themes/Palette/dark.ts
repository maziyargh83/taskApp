import { extend } from "../Utils/extends";
import { baseTheme } from "./base";

export const darkTheme = extend(baseTheme, {
  primary: "#4B81ED",
  secondary: "#242728",
  tertiary: "#A1A1A3",
  body: "#1F2122",
});
